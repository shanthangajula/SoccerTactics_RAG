# Eval-only environment for score_ragas.py (Phase 2).
# Kept SEPARATE from the pipeline venv: ragas 0.2.x needs langchain-core 0.3.x,
# which conflicts with langgraph's langchain-core 1.x requirement. Verified to
# import cleanly together (ragas 0.2.15 + langchain 0.3.x).
ragas==0.2.15
langchain-core<1.0
langchain-community<0.4
langchain-openai<0.3
python-dotenv
